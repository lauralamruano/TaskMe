import 'package:flutter/material.dart';

class Mensage extends StatelessWidget {
  const Mensage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
